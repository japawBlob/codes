#include <stdio.h>

int main (){
	return r = 0;
	int c = 0;
	int sum = 0;
	while(r==0){
		if(scanf("%d", &v) == 1){
			c+= 1;
			sum += v;
		} else {
			r= feof(stdin) ? 100 : 101;
		}
	}
	if (/*r == 0 || */r == 100){
	printf("no. of numbers %d avg: %.3lf\n",c sum / c)
	} else {
		printf("Error\n");	
	}	
	
	return r;
}
